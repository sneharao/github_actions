name: First Workflow

on: push

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome message
          run: echo 'My first actions'
        
        - name: Checkout Repo
          uses: actions/checkout@v4

        - name: List Files and read file
          run: |
             ls -ltra
             cat README.md

        - name: Generate ASCII Artwork
          run: cowsay -f dragon "Run for cover, I am a DRAGON....ARAWWW, THUSS THUSS" >> dragon.txt

        # - name: Read file
        #   run: cat README.md


